#include <cs50.h>
#include <stdio.h>


int main(int arrt[]){
    int length = sizeof(arrt) / sizeof(arrt[0]);
    if (length>0){

        for(int i=0;i<length; i++){
            for(int j=0;i<j){}
        }

    }
}
